# Optional: point the client at a deployed API. In development the Vite dev
# server proxies /api to http://localhost:4000 automatically, so you can leave
# this unset.
# VITE_API_URL=https://jobgraph-server.onrender.com/api
